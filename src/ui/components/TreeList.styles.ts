import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  itemContainer: {
    borderWidth: 1,
    borderRadius: 6,
    margin: 8,
    padding: 16,
    borderColor: '#DDD',
    flexDirection: 'row',
    alignItems: 'center',
  },

  itemTitle: {
    fontSize: 16,
  },
});
